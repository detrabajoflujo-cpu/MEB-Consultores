# Variables de entorno para el Frontend React
# Crea un archivo .env en frontend-react/ con estas variables

# URL del backend Java (por defecto localhost:8080)
VITE_API_URL=http://localhost:8080/api

# Usar datos mock (true = no necesita backend, false = usa backend real)
VITE_USE_MOCK=true
